version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 5.0   
    commands:
      - dotnet tool install -g Amazon.ElasticBeanstalk.Tools
  build:
    commands:
      - cd test-dotnet-app-mvc
      - dotnet eb package
artifacts:
  files:
    - ./test-dotnet-app-mvc/bin/Release/net5.0/publish/*
  discard-paths: yes
